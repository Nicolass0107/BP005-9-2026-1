#include <stdio.h>
int potencia(int b, int e);
int main() {
    printf("Resultado: %d\n", potencia(2, 3));
    return 0;
}
int potencia(int b, int e) {
    int r = 1;
    for(int i=0; i<e; i++) r *= b;
    return r;
}
