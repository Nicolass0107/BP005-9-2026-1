#include <stdio.h>
float promedio(float v[], int n);
int main() {
    float n[] = {4.0, 5.0, 3.0};
    printf("Promedio: %.2f\n", promedio(n, 3));
    return 0;
}
float promedio(float v[], int n) {
    float s = 0;
    for(int i=0; i<n; i++) s += v[i];
    return s/n;
}
