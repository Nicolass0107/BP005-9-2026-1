#include <stdio.h>
float area(float b, float h);
float peri(float b, float h);
int main() {
    float b = 5, h = 3;
    printf("Area: %.2f, Perimetro: %.2f\n", area(b,h), peri(b,h));
    return 0;
}
float area(float b, float h) { return b * h; }
float peri(float b, float h) { return 2 * (b + h); }
