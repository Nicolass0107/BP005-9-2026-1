#include <stdio.h>

int main() {
  int x, y;

  printf("ingrese el numero de x: ");
  scanf("%d", &x);
  printf("ingrese el numero y: ");
  scanf("%d", &y);

  if (x > y) {
    printf("%d es mayor que %d ",x, y);

  }

  return 0;
}
